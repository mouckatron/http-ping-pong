```
usage: pingpong.py [-h] [-s] [-sp SERVER_PORT] [-c] [-ch CLIENT]

optional arguments:
  -h, --help            show this help message and exit
  -s, --start-server    Start the server
  -sp SERVER_PORT, --server-port SERVER_PORT
                        Port for server to run on
  -c, --start-client    Start the client
  -ch CLIENT, --client CLIENT
                        The host for the client to connect to. May be used
                        multiple times. Format:
                        <hostname|ip>[:port][:loggingname]
```
